# DO NOT USE THESE LOGOS

These logos are only saved as an example on how not to do logos.

Do not use strokes on lines! Always expand the appearance of strokes!

Also, don't use rasterized shadow effects! Use gradients instead!
